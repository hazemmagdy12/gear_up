import 'package:flutter/material.dart';

class AppColors {
  // الألوان الأساسية
  static const Color primary = Color(0xFF1F5F8B);      // الأزرق بتاع زرار Login
  static const Color secondary = Color(0xFF2C2C2C);    // الأسود بتاع زرار Sign Up

  // الخلفيات والنصوص
  static const Color background = Color(0xFFFFFFFF);   // خلفية التطبيق بيضاء
  static const Color surface = Color(0xFFF3F4F6);      // الرمادي الفاتح (خلفية حقول الكتابة)

  // ألوان النصوص
  static const Color textPrimary = Color(0xFF111827);  // الأسود الغامق للعناوين
  static const Color textSecondary = Color(0xFF6B7280); // الرمادي للكلام الفرعي
  static const Color textHint = Color(0xFF9CA3AF);     // لون الـ Placeholder

  // الحدود (Borders)
  static const Color border = Color(0xFFE5E7EB);       // لون الحدود الفاتحة
}